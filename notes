props are just argument of function
passing a props to component is just like passing argument to functions
prop is a object.
backend data come to us in json form


#react Hooks
(Normal js utility function)
  useState()
  useEffect()

  whenevr a state variable update react will rerender my component

if you want to make ur component dynamic if u want something chnage in ur component you should use reacthook {useState} not just normal variable